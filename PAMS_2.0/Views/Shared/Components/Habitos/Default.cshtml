@model PAMS_2._0.Data.ViewModel;
@{
    ViewData["Title"] = "Habitos";
}




@if (Model.habitos == null)
{
    <p>No existen Registros.</p>

}
else if (Model.listaHabitos.Count() > 0)
{
    <div class="container">
        <div class="row">
            <div class="col-sm-12">
                <h3>Habitos</h3>
            </div>
        </div>
        <div class="row">
            <div class="col-sm-10">
                @if (TempData["message"] != null)
                {
                    <div class="alert alert-warning alert-dismissible fade show " role="alert">
                        @TempData["message"]
                        <button type="button" class="close" date-dismiss="alert" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                        </button>
                    </div>
                }
                
            <form method="post" asp-action="Edit" asp-controller="Hab">
                <input type="hidden" asp-for="@Model.habitos.idPaciente" />
                <input type="hidden" asp-for="@Model.habitos.id" />
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                <div class="form-group">
                    <label asp-for="@Model.habitos.alimento" class="control-label"></label>
                    <textarea asp-for="@Model.habitos.alimento" class="form-control"></textarea>
                    <span asp-validation-for="@Model.habitos.alimento" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <label asp-for="@Model.habitos.antPsicologicos" class="control-label"></label>
                    <textarea asp-for="@Model.habitos.antPsicologicos" class="form-control"></textarea>
                    <span asp-validation-for="@Model.habitos.antPsicologicos" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="@Model.habitos.dream" class="control-label"></label>
                    <textarea asp-for="@Model.habitos.dream" class="form-control"></textarea>
                    <span asp-validation-for="@Model.habitos.dream" class="text-danger"></span>
                </div>
              
                <div class="form-group">
                    <input type="submit" value="Actualizar Datos" class="btn btn-primary" />
                </div>
            </form>
            </div>
        </div>

    </div>

}

@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}