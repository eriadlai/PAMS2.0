@model PAMS_2._0.Data.ViewModel;
@{
    ViewData["Title"] = "Habitos";
}




@if (Model.reporteSesion == null)
{
    <p>No existen Registros.</p>

}
else if (Model.listaReporteSesion.Count() > 0)
{
    <div class="container">
        
        <div class="row">
            <div class="col-sm-10">
                @if (TempData["message"] != null)
                {
            <div class="alert alert-warning alert-dismissible fade show " role="alert">
                @TempData["message"]
                <button type="button" class="close" date-dismiss="alert" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>                }
                <hr />
                <div class="row">
                    <div class="col-sm-12">
                        <h5>Reporte de Sesion</h5>
                    </div>
                </div>
                <form method="post" asp-action="Edit" asp-controller="RepSesion">
                    <input type="hidden" asp-for="@Model.reporteSesion.idPaciente" />
                    <input type="hidden" asp-for="@Model.reporteSesion.id" />
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>

                    <div class="form-group">
                        <label asp-for="@Model.reporteSesion.notaSesion" class="control-label"></label>
                        <textarea asp-for="@Model.reporteSesion.notaSesion" class="form-control"></textarea>
                        <span asp-validation-for="@Model.reporteSesion.notaSesion" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="@Model.reporteSesion.diagnostico" class="control-label"></label>
                        <textarea asp-for="@Model.reporteSesion.diagnostico" class="form-control"></textarea>
                        <span asp-validation-for="@Model.reporteSesion.diagnostico" class="text-danger"></span>
                    </div>

                    <div class="form-group">
                        <label asp-for="@Model.reporteSesion.observaciones" class="control-label"></label>
                        <textarea asp-for="@Model.reporteSesion.observaciones" class="form-control"></textarea>
                        <span asp-validation-for="@Model.reporteSesion.observaciones" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <label asp-for="@Model.reporteSesion.objetivoClinico" class="control-label"></label>
                        <textarea asp-for="@Model.reporteSesion.objetivoClinico" class="form-control"></textarea>
                        <span asp-validation-for="@Model.reporteSesion.objetivoClinico" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <input type="submit" value="Actualizar Datos" class="btn btn-primary" />
                    </div>
                </form>
            </div>
        </div>

    </div>

}

@section Scripts{
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}